import { MyHealthRecordWorkspace } from "@/components/medical/my-health-record-workspace"

// Renders the Student's own compliance record without mutation controls.
export default function StudentMyHealthRecordPage() {
  return <MyHealthRecordWorkspace />
}
