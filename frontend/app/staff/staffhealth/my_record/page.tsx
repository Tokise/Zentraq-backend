import { MyHealthRecordWorkspace } from "@/components/medical/my-health-record-workspace"

// Renders the Staff user's own compliance record without mutation controls.
export default function StaffMyHealthRecordPage() {
  return <MyHealthRecordWorkspace />
}
