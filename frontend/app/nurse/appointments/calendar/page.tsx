"use client";

import { AppointmentsCalendarPage } from "@/app/admin/appointments/calendar/page";

// Displays the assigned nurse's appointments in the shared calendar workspace.
export default function NurseAppointmentsCalendarPage() {
  return <AppointmentsCalendarPage />;
}
