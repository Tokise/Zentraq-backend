"use client";

import { AppointmentsCalendarPage } from "@/app/admin/appointments/calendar/page";

// Displays the assigned doctor's appointments in the shared calendar workspace.
export default function DoctorAppointmentsCalendarPage() {
  return <AppointmentsCalendarPage />;
}
