import { MyHealthRecordWorkspace } from "@/components/medical/my-health-record-workspace"

// Renders the Faculty user's own compliance record without mutation controls.
export default function FacultyMyHealthRecordPage() {
  return <MyHealthRecordWorkspace />
}
