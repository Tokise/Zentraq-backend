import { MySchedulePage } from "@/components/scheduling/my-schedule"

// Shows the signed-in doctor's own published schedule.
export default function DoctorMySchedulePage() {
  return <MySchedulePage />
}
